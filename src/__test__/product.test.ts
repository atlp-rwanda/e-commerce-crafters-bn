import { updateProduct } from "../controllers/product.controller";
import { deleteProduct } from "../controllers/product.controller";

describe('updateProduct', () => {
    test("Should return success message and object", async() => {
        

    })

    it('should return 403 if permission fails')
    it('should return 404 if product not found')
    it('should update product and return 200 if successful')
})

describe('deleteProduct', () => {
    test("Should return success message", async() => {
        

    })

    it('should return 403 if permission fails')
    it('should return 404 if product not found')
    it('should delete product and return 200 if successful')
})